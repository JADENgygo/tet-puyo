# AGENTS.md

このファイルは、このリポジトリで作業するコーディングエージェント向けのガイドです。

## プロジェクト概要

- プロジェクト名: `tet-puyo`
- 現時点では初期状態です。構成や技術スタックが決まったら、この文書も更新してください。

## 作業方針

- 変更前に関連ファイルと既存の実装方針を確認する。
- ユーザーの依頼に必要な範囲だけを変更し、無関係なリファクタリングは避ける。
- 既存の命名規則、コードスタイル、ディレクトリ構成を優先する。
- 秘密情報、認証情報、個人用の設定ファイルをコミットしない。
- 生成物や依存パッケージは、必要な場合を除いて直接編集しない。

## 実装と検証

- 新しい挙動には、可能な限りテストを追加または更新する。
- 変更後は、利用可能なテスト、lint、型チェック、ビルドを実行する。
- 検証コマンドが未整備の場合は、実施できなかった項目を作業報告に明記する。
- バグ修正では、原因と修正内容が対応するようにし、可能なら再発防止テストを追加する。

## Git運用

- ユーザーが作成した未コミットの変更を上書きまたは破棄しない。
- `git reset --hard` などの破壊的な操作は行わない。
- コミット、push、ブランチ作成は、明示的に依頼された場合のみ行う。
- 変更内容は小さく、レビューしやすい単位に保つ。

## ドキュメント

- セットアップ手順、公開API、設定、操作方法に影響する変更では、関連ドキュメントも更新する。
- 新しいコマンドや環境変数を追加した場合は、用途と使用例を記載する。

## 完了時の報告

- 変更した内容を簡潔にまとめる。
- 実行した検証とその結果を記載する。
- 未解決事項や追加対応が必要な点があれば明記する。

## プルリクエストの作成方法

- devブランチを最新にする
- devブランチを基準にfeature/xxxブランチを作成する
- xxxは実装機能名を示す名前
- feature/xxxブランチからdevブランチへのプルリクエストを作成しマージする
- 可能な限り日本語を使う
